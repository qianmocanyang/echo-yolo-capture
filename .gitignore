# ---- Python --------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- 虚拟环境 ------------------------------------------------------------
.venv/
venv/
env/

# ---- 打包产物 ------------------------------------------------------------
build/
dist/
*.spec.bak

# ---- 图标临时预览 --------------------------------------------------------
# assets/echo.ico 与 docs/screenshot.png 是提交的；
# 调试用的预览图与临时截图不进仓库。
assets/_preview_*.png
assets/_*_shot.png

# ---- 运行产物 ------------------------------------------------------------
# 截图数据集一律由用户指定保存目录，不落在仓库里；
# 下面这些是本地调试时可能出现在项目根目录的残留。
*.log
metadata/
exports/
images/
labels/
*.lock
